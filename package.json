{
    "name": "fullstack-project-express-nextjs",
    "version": "1.0.0",
    "main": "index.js",
    "repository": "https://github.com/Duran24062005/fullStack-project-express-react.git",
    "author": "Alexi_Dg <alexisdurangomez588@gmail.com>",
    "license": "MIT",
    "scripts": {
        "dev": "concurrently \"./.venv/bin/python -m uvicorn backend.app.main:app --reload --port 8000\" \"npm run dev --prefix frontend\"",
        "build": "npm run build --prefix frontend",
        "start": "concurrently \"./.venv/bin/python -m uvicorn backend.app.main:app --port 8000\" \"npm run start --prefix frontend\"",
        "install:all": "./.venv/bin/pip install -r backend/requirements.txt && npm install --prefix frontend"
    },
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}